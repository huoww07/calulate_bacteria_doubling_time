from setuptools import setup

setup(name='Doubling_time_Modularized',
      version='0.1',
      description='Doubling time calculation',
      packages=['Doubling_time_Modularized'],
      zip_safe=False)
